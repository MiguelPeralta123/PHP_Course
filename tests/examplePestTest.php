<?php

// These two tests are the same (test and it). The first argument is the description and the second one is the test

/*test('test true in pest', function() {
    $working = true;
    expect($working)->toBeTrue();
});

it('it test true in pest', function() {
    $working = true;
    expect($working)->toBeTrue();
});*/